/**
 * Placeholder proving the domain import path and test mirror.
 * Replaced by F1 (frame.ts — the framing protocol) per the sequenced dev plan.
 * Domain layer: ZERO external dependencies — pure business logic only.
 */
export function domainLayerName(): string {
  return 'domain';
}
