/**
 * Placeholder proving the presentation import path and test mirror.
 * Replaced by F9/F10 (CLI surface, diagnostics view).
 * Presentation layer: parsing/serialisation only — calls application ONLY.
 */
export function presentationLayerName(): string {
  return 'presentation';
}
