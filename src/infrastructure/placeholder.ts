/**
 * Placeholder proving the infrastructure import path and test mirror.
 * Replaced by F4–F6 (replay client, signaling client, peer connection).
 * Infrastructure layer: all I/O lives here; implements domain interfaces.
 */
export function infrastructureLayerName(): string {
  return 'infrastructure';
}
