# Beam — environment template. Placeholder keys ONLY; real values are
# injected at shell level and NEVER written to disk (CLAUDE.md §3).
# All env access goes through src/config.ts — never process.env in feature code.
# Adding a key here is part of the new-environment-variable HARD STOP.
BEAM_LOG_LEVEL=info
APP_PORT=8080
