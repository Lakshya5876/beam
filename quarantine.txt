# Quarantine — flaky tests, committed team state.
#
# Guide §6 T4: flaky tests are quarantined HERE with a linked issue —
# never deleted, never retried-until-green inside the covenant. /review
# prints the quarantine count and the modules it covers on every run
# ("QUARANTINED: 3 tests covering money.py, auth.py"). A quarantined test
# that covers a CORE_FILES module (see CLAUDE.md) is a HARD STOP, not a
# deferral — that is the one test that would catch a core regression.
#
# Format: <package>/<test file>::<test name> | issue=<link> | since=<YYYY-MM-DD>
#
root/tests/infrastructure/signaling-client.test.ts::afterAll (nodeDataChannel.cleanup) | issue=TBD-ask-before-filing | since=2026-08-22
# Not a specific it() — every one of this file's 14 assertions passes
# deterministically every time, including on CI. The file's own afterAll
# hook's native-library cleanup() intermittently throws "libdatachannel
# error# cleanup timeout (possible deadlock)" after ~10s on GitHub Actions'
# ubuntu-latest runners specifically; unreproducible on Windows, including
# under a forced single-process vitest run matching CI's file-batching
# shape. Root-caused as far as possible without a real Linux repro
# environment — see tests/fixtures/native-cleanup.ts's doc for the full
# investigation (two genuine, independent correctness fixes were made along
# the way and kept regardless: a double-cleanup-invocation guard, and
# signaling-client.ts's disconnect() now genuinely awaiting native close
# confirmation instead of resolving early). Mitigated (not merely hidden) by
# catching the native cleanup() failure and logging it rather than letting
# post-hoc process hygiene fail an otherwise fully-passing suite — see
# tests/fixtures/native-cleanup.test.ts for regression coverage of that
# catch. Re-open/upstream if this recurs with a different symptom.
