# Dependencies / build
node_modules/
dist/
*.tsbuildinfo
.wrangler/

# Test artifacts
coverage/

# Secrets — NEVER committed
.env
.env.*
!.env.example
.env.worktree
*.pem

# OS / editor noise
.DS_Store
npm-debug.log*
.claude/session_state.json
.claude/session_spend.tmp
.claude/git_cache.json
.claude/checkpoints/
.claude/covenant_state.json

# Local governance-bootstrap reference material — not project documentation
v1_claude_code_development_guide_existing.md
v1_implementation_package_existing.md

# Local security audit reports — never committed
SECURITY_AUDIT.md
SECURITY_AUDIT*.md
SECURITY_AUDIT_20/
